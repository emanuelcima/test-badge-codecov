language: python
python:
  - "3.8"
cache: pip3
install:
  - pip3 install -r requirements.txt
  - pip3 install codecov
  - pip3 install coverage
script:
  - coverage run test.py
after_sucess:
  - codecov